class RequestWidgetItemModel {
  final String iconPath;
  final String title;
  final String route;

  const RequestWidgetItemModel({
    required this.iconPath,
    required this.title,
    required this.route,
  });
}